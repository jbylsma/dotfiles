# Global Rules

- Columns in Markdown tables should be evenly spaced.
- Never run `sudo` commands. If a command requires `sudo`, prompt me to run it manually.
- Don't include Claude as a co-author on Git commits.
